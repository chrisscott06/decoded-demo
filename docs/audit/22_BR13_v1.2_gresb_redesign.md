# Brief 22 — GRESB Visualisation Module v1.2 — audit

**Status:** Active. Task 1 PASS (JSON drop-in verified, page loads, console clean).
**Brief:** [docs/briefs/active/22_BR13_v1.2_gresb_redesign.md](../briefs/active/22_BR13_v1.2_gresb_redesign.md).
**Supersedes:** Brief 20 ([docs/briefs/archive/20_BR13_gresb_phase1_SUPERSEDED.md](../briefs/archive/20_BR13_gresb_phase1_SUPERSEDED.md)). Tasks 1-3 of Brief 20 stay live in code (atoms + JSON wiring + initial Overview/Forward pages); Tasks 4-7 are obsoleted by this brief's redesign.

## Numbering note

Authored as "Brief 13 v1.2" by NZA (internal sequence BR-13). Filed locally as Brief 22 because the local sequence has Brief 13 (chart fixes Phase 1B) and Brief 20 (the v1.0 attempt at BR-13, now archived). Keeping the local count monotonic.

## Task 1 — Drop in v1.2 enriched JSON — PASS (this commit)

- `eir/src/data/gresb.json` replaced with the v1.2 enriched version (2976 lines, includes per-aspect `colour`, populated `whatItMeans` / `whatWeNeedToDo` / `decision2025` fields per indicator, `defendable2026` + `achievable2026` new names PLUS `floor2026` + `ceiling2026` kept as backward-compat aliases).
- Existing GresbOverview + GresbForward components from Brief 20 Tasks 1-3 still render unchanged (they read floor/ceiling). Browser walkthrough at 1920×1080 confirms `/gresb/overview` loads, h1 "GRESB 2026" renders, 6 subsection h3s present, zero console errors.
- starBands + forwardPlanning intact; 15 aspects with new `colour` field per aspect.

**Backward-compat strategy:** keeping the old field names as aliases means the existing Overview keeps working unchanged while Task 3 rebuilds it. No "big bang" needed for the rename — the rebuild can introduce defendable/achievable references at its own pace.

## Tasks 2-7 — pending

(populated as each task closes)
